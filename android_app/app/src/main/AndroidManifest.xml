<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission to access the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FinancasApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Financas"
        tools:targetApi="31"
        android:usesCleartextTraffic="true" > <!-- Allow HTTP for local development. Disable for production. -->

        <meta-data android:name="io.sentry.dsn" android:value="YOUR_SENTRY_DSN" />

        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true" />

        <activity
            android:name=".ui.transactions.AddTransactionActivity"
            android:exported="false" />

        <activity
            android:name=".ui.reports.ReportsActivity"
            android:exported="false" />

    </application>

</manifest>
